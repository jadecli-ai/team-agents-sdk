# ── Organization Keys (shared across projects) ──────────────────────
ORG_ANTHROPIC_API_KEY=           # Anthropic API key for Claude Agent SDK
ORG_GITHUB_TOKEN=                # GitHub PAT (or use gh auth login instead)
ORG_NEON_API_KEY=                # Neon platform API key (org-level)

# ── Project Keys (specific to jadecli-team-agents-sdk) ──────────────
PRJ_NEON_DATABASE_URL=           # Neon connection string (postgresql+asyncpg://...)
PRJ_VERCEL_TOKEN=                # Vercel deploy token for jadecli.com
PRJ_GITHUB_REPO=                 # GitHub repo (e.g. jadecli/team-agents-sdk)
PRJ_GITHUB_PROJECT_NUMBER=       # GitHub Projects v2 number
PRJ_MLFLOW_TRACKING_URI=         # MLflow server URL (e.g. http://localhost:5000)
PRJ_MLFLOW_EXPERIMENT_NAME=      # MLflow experiment name
PRJ_CLONE_ID=                    # WSL2 clone identity (set by clone-init)
PRJ_MLFLOW_SYNC_ENABLED=         # Enable MLflow SQLite->Neon sync (true/false)

# ── Cache Providers (all active — fastest healthy wins) ─────────────
PRJ_UPSTASH_REDIS_URL=             # Upstash Redis (rediss://default:...@...:6379)
PRJ_REDIS_CLOUD_URL=               # Redis Cloud (rediss://default:...@...:port)
PRJ_DRAGONFLY_CLOUD_URL=           # Dragonfly Cloud (redis://...@...:6379)
PRJ_AIVEN_DRAGONFLY_URL=           # Aiven for Dragonfly (rediss://default:...@...:port)
PRJ_DRAGONFLY_URL=redis://localhost:6379/0  # Local fallback (redis://localhost:6379/0)
