# Tailscale startup flags for WSL2 clones
# All WSL2 distros share one network namespace, so use userspace networking
# to avoid routing conflicts with the Windows host.
TAILSCALE_FLAGS="--tun=userspace-networking --netfilter-mode=off"
